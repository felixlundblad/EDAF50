/*
 * Class Palindrome:
 * Controls wether an input is a palindrome or not
 */
#ifndef PALINDROME_H
#define PALINDROME_H

using namespace std;

#include <string>

class Palindrome
{
    public:
    Palindrome(std::string s);
    bool isPalindrome();
    private:
    protected:
};

bool isPalindrome();

#endif